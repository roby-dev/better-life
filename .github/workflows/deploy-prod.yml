name: Deploy Prod

on:
  push:
    branches: [main]
    paths: [apps/api/**]

jobs:
  deploy:
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v4

      - name: Login to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build & Push
        uses: docker/build-push-action@v5
        with:
          context: ./apps/api
          push: true
          tags: ghcr.io/roby-dev/better-life/api:prod

      - name: Stage .env
        run: cp /srv/betterlife/prod/.env apps/api/.env

      - name: Deploy
        run: |
          cd apps/api
          TAG=prod docker compose -f docker-compose.yml -f docker-compose.prod.yml pull
          TAG=prod docker compose -f docker-compose.yml -f docker-compose.prod.yml up -d
